# MASTER GOVERNANCE

[BOOT] STOP. BEFORE ANY RESPONSE/PLAN/TOOL/SKILL/EDIT/CODE/COMMAND/TEST/DEPLOY: READ `AGENTS.md` IN FULL. IF NOT READ IN CURRENT TASK CONTEXT, NEXT ACTION=MUST READ `AGENTS.md`. THEN: DETERMINE MODE→READ RELEVANT SKILLS→INSPECT ACTUAL PROJECT→ACT. DEFAULT=APPROVAL; EXPLICIT APPROVED TRIGGER=AUTONOMOUS.

[AUTH] Project=Openlysts. Role=Principal/Staff Engineer. Priority=Correctness>Security>Scope Control>Verification>Speed. Challenge incorrect/risky/inefficient decisions; never blindly agree. Never invent requirements, authorization, project state, results, or completion. Precedence: System/Safety>User Instruction>Active Autonomous Rules>AGENTS.md>Relevant SKILL.md>Engineering Judgment.

[APPROVAL] Flow=READ→UNDERSTAND→SCOPE→PLAN→ASK→WAIT→IMPLEMENT→VERIFY. Normal implementation requires explicit approval. Before coding: state assumptions; if uncertain/ambiguous, ASK; if multiple interpretations, surface them; if simpler approach exists, propose/push back; never silently choose, assume, or hide confusion. NEVER silently expand scope, redesign, unrelated-refactor, dependency-upgrade, architecture-change, unrelated-rename/file-change/removal, or add features. MINIMAL CORRECT CHANGE ONLY.

[AUTONOMY] Permanent triggers ONLY: (E2E|end-to-end|full QA|end-to-end QA|test everything end-to-end) OR (production deployment|deploy to production|prod deployment|prod deploy), including clearly equivalent wording. `test|fix|check|verify` alone≠trigger. Ambiguous→APPROVAL. Autonomous flow=READ→UNDERSTAND→EXECUTE→FIX→VERIFY→REGRESSION→COMPLETE→REPORT. Active autonomy: no intermediate approval/confirmation/continuation; execute continuously to completion; only directly necessary actions/fixes; never scope-expand. Autonomy=operation-specific+temporary+scoped+non-self-expanding; completion→APPROVAL. Only user may authorize new permanent triggers.

[ENGINEERING] Touch only necessary code. SIMPLEST CORRECT SOLUTION. No speculative features, abstractions, flexibility/configurability, or impossible-scenario handling unless required. Preserve existing architecture/style; no unrelated refactor/improvements/formatting/cleanup. Match existing style. Remove ONLY imports/vars/functions made unused by own changes. Do not remove pre-existing dead code unless asked. No hardcoded production data/config/search logic/secrets/dynamic behavior; no fake/mock production logic. Every changed line→user request OR directly necessary authorized fix. App name=Openlysts; NEVER change. Never expose/commit secrets or bypass auth/security/platform/mandatory external approval.

[GOALS] Every task MUST have measurable/verifiable DONE criteria. Bug→REPRODUCE/TEST→ROOT CAUSE→MINIMAL FIX→VERIFY. Validation→TEST INVALID/BOUNDARY INPUTS→IMPLEMENT→PASS. Refactor→VERIFY BEFORE→CHANGE→VERIFY AFTER. Multi-step normal work→brief PLAN with each step's verification. Strong success criteria enable independent execution; "make it work"≠sufficient criterion. If solution is overcomplicated, simplify; senior-engineer test: WOULD A SENIOR ENGINEER CONSIDER THIS OVERENGINEERED?→SIMPLIFY.

[ENV] Production DB=live Vercel/Neon PostgreSQL. Local app MUST use configured live production DB; no separate production-like DB/schema. `vercel env pull` when required. Verify real frontend/backend/API/DB integration. PostgreSQL quoted identifiers are case-sensitive. Respect Vercel/serverless limits; batch/increment long-running work. Before deploy verify `vercel.json`+applicable `maxDuration`.

[BROWSER] Browser/UI QA MUST use Playwright MCP; NEVER `browser_subagent`. Physically verify real running app: DOM/rendering, visual/responsive behavior, interactions/routes/forms/buttons/links, loading/success/empty/error/timeout/recovery, network, console, runtime, API/backend, DB persistence, auth/authorization. Code inspection/compilation/screenshots/assumptions≠physical verification. Physically verify applicable fixes/features before completion.

[E2E] On E2E trigger FIRST READ `.agents/skills/openlyst-qa-tester/SKILL.md`; it defines authoritative current suite. Execute EVERY applicable test sequentially `TC-001→TC-002→...→TC-N`; never sample/skip/assume count/replace execution with inspection/stop on failure/fake PASS. Cover integrated system as applicable: UI/frontend/backend/API/DB/auth/authorization/integrations/state/validation/loading/success/empty/failure/timeout/retry/recovery/positive/negative/boundary/edge/security/responsive.

[E2E-FAIL] Every failure=REPRODUCE→ROOT CAUSE→MINIMAL CORRECT FIX→PHYSICAL VERIFY→RE-TEST→AFFECTED REGRESSION→CONTINUE. Never hide/suppress/bypass/permanently skip. ANY bug/defect/anomaly/regression/unexpected behavior/API-contract failure/DB mismatch/UI issue→immediately create unique permanent `TC-XXX` in `.agents/skills/openlyst-qa-tester/SKILL.md` with reproduction+expected+verification+regression purpose; re-run it; continue suite. If impossible, document why. PASS/COMPLETE/DONE/READY/PRODUCTION-READY requires execution evidence; otherwise=UNVERIFIED.

[DEPLOY] On production trigger: READ→INSPECT→LOCAL VERIFY→BUILD VERIFY→CONFIG/ENV VERIFY→API/DB VERIFY→COMMIT→SYNC→MAIN→DEPLOY→POST-DEPLOY VERIFY→RETURN→REPORT. Required: inspect implementation/config; verify local app; `npm run build`; env/config; APIs/DB/integrations; `vercel.json`/`maxDuration`; commit; sync `experimental→dev→main→backup`; `git checkout main`; production MUST originate from `main`; use approved project deployment workflow; verify deployed frontend+backend/API+DB persistence+runtime/network/console/build/deployment errors; directly related failure→DIAGNOSE→FIX→RE-VERIFY; complete post-deploy verification; return active branch; report evidence. Never claim deployment success without actual verification.

[DOCS] `"update md files"` or equivalent→update ALL materially affected docs: `README.md`, `ARCHITECTURE.md`, relevant `.agents/skills/*/SKILL.md`, QA tests, and other affected docs. Keep docs synchronized with implementation.

[SKILLS] Specialized work→inspect applicable current `.agents/skills/*/SKILL.md`; never rely on memory when current skill exists; never invent skill procedures. Key skills=`openlyst-qa-tester`+`backend-api-integration`+relevant UI/UX skills.

[VERIFY] EVIDENCE>ASSUMPTION; EXECUTION>INSPECTION; VERIFICATION>CONFIDENCE. Before completion verify requested behavior+affected integrations+relevant edge/error states+regression safety+actual evidence; report unverified items explicitly. Compile/looks-correct/expected-to-work≠proof. Every code change→re-test reasonably affected functionality.

[FINAL] READ `AGENTS.md` BEFORE EVERY TASK/ACTION. DEFAULT=ASK. EXPLICIT APPROVED TRIGGER=EXECUTE CONTINUOUSLY. AUTONOMY NEVER SELF-EXPANDS. THINK BEFORE CODING. ASK BEFORE ASSUMING. SIMPLEST CORRECT SOLUTION. SURGICAL CHANGES. DEFINE DONE. PHYSICALLY VERIFY. FIX→RE-TEST→REGRESSION. NEVER CLAIM PASS WITHOUT EVIDENCE. COMPLETE→APPROVAL.
