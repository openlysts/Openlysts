import 'dotenv/config'; import { db } from '../server/db/index.js'; async function main() { const res = await db.query('SELECT COUNT(*), email_normalized FROM "User" GROUP BY email_normalized HAVING COUNT(*) > 1'); console.log(res.rows); process.exit(0); } main();
